# Incident & Bug Ledger

> Append-only журнал. Каждый реальный баг → строка.
> Агент перед работой проверяет журнал по своей области.

| Date | Area | Symptom | Root cause | Fix | Guard |
|------|------|---------|------------|-----|-------|
| 2026-07-27 | builder | Параллельная обработка файлов через БД-чанки ломала всё 2 раза подряд: лаги, долгая отправка, десинхронизация между админами | 1) SQLite single-writer — множественная запись чанков блокировала все остальные запросы. 2) Отсутствие контроля concurrent AI-вызовов — 10+ generateText() одновременно. 3) React Query не синхронизирует мутации между клиентами | 1) better-queue (max 3 concurrent) + BuilderJob table. 2) UploadedFile — весь текст файла одной записью, чанки через slice() в памяти. 3) SSE-push для удаления сообщений (builder_message_deleted) | G21, G22, G23, G24 |
| 2026-07-27 | ui | На мобилке табы Documents не доскролливались до конца из-за overflow-кнопки «...» antd Tabs | antd Tabs рендерит `ant-tabs-nav-operations` (кнопку «...») которая занимает место справа и визуально блокирует скролл | `ant-tabs-nav-operations { display: none }`, `ant-tabs-nav-list { overflow-x: auto; transform: none }` | ANTI-PATTERNS: UI / Страницы |
| 2026-07-27 | ui | Дизайн шапок всех страниц различался: разные font-size (13/16/18px), padding, border. Мобильная кнопка меню на отдельных рядах или отсутствовала | Каждая страница использовала свои inline-стили для заголовка. Не было единого компонента | `<PageHeader>` компонент + `MobileMenuProvider` контекст. Все страницы переведены на единый компонент | G25 |
| 2026-07-27 | sidebar | Дубликат «AI Master» в ChatNav и AdminSection — два пути на одну страницу | Навигационный пункт `/admin/ai-settings` был добавлен в обе секции | Удалён из ChatNav, оставлен только в AdminSection | ANTI-PATTERNS: UI / Страницы |
| 2026-07-28 | builder | Десинхронизация между админами: сообщения и файл-бар появлялись в разное время на разных клиентах. Бот-бабл работал синхронно (SSE), а сообщения и файлы — нет | React Query `invalidateQueries()` работает локально (per-browser). Мутации (send message, upload/remove file) не имели SSE-бродкаста — другие клиенты узнавали о них только при следующем poll/refetch или при завершении AI-обработки | Добавлены SSE-события: `builder_message_sent`, `file_uploaded`, `file_removed`. Shell слушает их и инвалидирует соответствующие queryKey. Логика синхронизации унифицирована с бот-баблом | G24, правило: **любая мутация чата/файлов затрагивающая других админов — всегда SSE-push.** Проверять: создание/удаление/изменение в builder-чате → есть ли broadcast? |
| 2026-07-29 | builder, sse | Парсинг PDF вешал весь Next.js, SSE polling создавал десинхронизацию. С 2026-08-08 PDF-парсинг полностью удалён — заменён на .md архивный импорт через adm-zip + explore_archive. | 1) pdf2json — CPU-блокировка. 2) `setTimeout(poll,300)` в steps-SSE. 3) `clearSession` в send-message стирал буфер парсинга. 4) `close()` в onerror. 5) `refetchInterval:3000` | 1) Полное удаление pdf2json + file-parser, замена на adm-zip + архивный импорт. 2) EventEmitter push (`onStep`). 3) `clearSession` убран. 4) `close()` убран. 5) `refetchInterval` убран. `prisma.update` с `await` | G22, G23, G26, G27 |
| 2026-07-29 | builder, llm | Агент галлюцинировал brain-документы ("7 готово" при пустой БД), пропускал чанки без документирования, останавливался после первого файла | 1) Промпт не требовал проверки через search_documents. 2) Рекомендательный тон ("Create or update..."). 3) Brain — "if contains instructions". 4) auto-continue не говорил "все файлы" | 1) STUDY MODE Step 5: обязательный `search_documents(brain)`. 2) Императивный промпт: `MANDATORY`, `BLOCKING RULE`. 3) Brain-типы — обязательный чеклист (3b). 4) auto-continue: "Process ALL files" | G28, ANTI-PATTERNS: LLM / Prompts |
| 2026-08-02 | builder | STUDY-режим останавливался на ~40 стр. (80 шагов). После удаления isStepCount(80) — останавливался на 1 шаге, отказывался продолжать | 1) `isStepCount(80)` — жёсткий лимит, 80 шагов / ~6 шагов на чанк ≈ 13 чанков × 5000 симв ≈ 40 страниц. 2) `stopWhen: undefined` в JS равен отсутствию аргумента — AI SDK v7.0.37 подставляет дефолт `isStepCount(1)`. 3) После 3×80=240 шагов компрессия TAIL=10 уничтожала контекст — агент терял ориентацию и сдавался | 1) `isLoopFinished()` вместо `undefined` для STUDY (никогда не останавливает цикл). Остановка только по AbortController или естественному финишу LLM. 2) CHAT сохраняет `isStepCount(80)` | Агент перед передачей `undefined` в библиотечную функцию с дефолтным параметром — сверять с сигнатурой: не равен ли `undefined` отсутствию аргумента |


| 2026-08-11 | game, dice | AI не вызывал `present_roll_check` в personal chat — галлюцинировал текст "жми на кнопки" вместо реального вызова тулза | DeepSeek v4 через кастомный роутер ненадёжно обрабатывает tool calls. Системный промпт описывал тулз но модель игнорировала | 1) Агрессивный промпт: "players have NO ability to roll. You are the ONLY gatekeeper". 2) `@dice-roller/rpg-dice-roller` v5.5.1 интегрирован вместо ручного regex. 3) `get_rolls/remove_roll/confirm_rolls` тулзы для управления | G29, ANTI-PATTERNS: LLM / Tools |
| 2026-08-11 | game, dice | После `confirm_rolls` AI терял все результаты бросков (consumed=true) — не мог ответить какие значения выпали | consumed-флаг скрывает роллы от AI, а результаты не сохранялись в game_hidden. AI не знал что нужно документировать ДО confirm | `write_note` в промпте перед confirm_rolls. G29 добавлен | G29 |
| 2026-08-11 | game, dice | Броски в чате сортировались по createdAt (назначение), а не по completedAt (клик) — результаты появлялись не там где игрок нажал | Сортировка использовала `createdAt` для completed роллов | `completedAt` добавлен в TSessionRoll, сортировка по нему для completed | — |
| 2026-08-11 | game, ui | Ролл-стрип не показывал кнопки assigned роллов | `getPersonalRollsAction` фильтровал `status: "completed"` — assigned не возвращались | Фильтр сменён на `status: { not: "cancelled" }`, клиент фильтрует completed/assigned раздельно | — |
| 2026-08-16 | game, sse | Бабл обдумывания не появлялся или не закрывался; броски не отображались до перезагрузки | 1) stale `setTimeout(clearSession, 10s)` от предыдущего батча стирал состояние нового батча → `emit` дропал `started`/`done`. 2) ручной реконнект (`es.close()` в onerror) ломал авто-реконнект EventSource. 3) три SSE-эндпоинта × вкладки = лимит 6 соединений HTTP/1.1 | 1) step-tracker → снапшот-модель без таймеров (терминальное событие удаляет запись). 2) нативный авто-реконнект EventSource, `onerror` пустой. 3) единый `/api/stream` (мультиплекс `ns: events|steps`) + resync на `onopen` | ANTI-PATTERNS: Real-time / SSE |
| 2026-08-16 | game, dice | Результат броска показывался громоздко (сумма / разбор всех кубиков), индивидуальные значения терялись | Результат хранился как число (`resultTotal Int`) + парсинг-костыли в UI | `Roll.result` — компактный текст (значения), `Roll.detail` — полный разбор на hover; одна кнопка через `count=N`; формула решает формат | ANTI-PATTERNS: Real-time / SSE, CODING: Броски |
| 2026-08-16 | game, docs | Список документов и открытый файл не обновлялись в реалтайме; открытый удалённый файл не закрывался | Не было SSE-событий на создание/изменение/удаление документов | Добавлены `document_created/updated/deleted`; auto-close модалки и редирект страницы при удалении открытого документа | G18, ANTI-PATTERNS: Real-time / SSE |
| 2026-08-19 | builder, db | Загрузка архива (8592 файла) вешала весь Next.js на 5-10 минут | `bulk_import_to_glossary` делал цикл `findFirst`+`create`/`update` на каждый файл ≈ 17 000 последовательных SQLite-запросов → write-lock замораживал сайт. Также `explore_archive` имел O(n²) вложенный цикл по папкам | Пакетный upsert: один `findMany` title + `createMany`/`updateMany` чанками по 500 (~20 запросов). explore_archive — линейный обход. WAL + busy_timeout включены в prisma.ts | G32, G33, ANTI-PATTERNS: Builder Agent |
| 2026-08-19 | builder, chat | Новые сообщения в билдер-чате не появлялись (в БД были, в UI нет) | `getBuilderMessagesAction` сортировал `asc` + take 30 → брал САМЫЕ СТАРЫЕ; при 32+ сообщениях новые уходили на страницу 2 | `desc` + take + reverse; клиент сортирует по возрастанию. Та же ошибка была во всех трёх агент-контекстах (модель не видела новые сообщения) | G38 |
| 2026-08-19 | game, docs | Игроки не могли открыть glossary-ссылки из чата мастера («Document not found») | `getDocumentAction` для не-админа разрешал только `game_visible`; GM по промту ссылается на glossary | Игроку с gameAccess разрешены `game_visible` + `glossary`; brain/game_hidden остаются мастеру | G37 |
| 2026-08-19 | auth, net | Вход по HTTP (LAN/Hamachi) «не работал», по HTTPS — серый экран; «перекидывает на localhost» | Cookie сессии ставилась `secure: true` в production → браузер не отправляет по HTTP. localhost-переходы — из-за абсолютных URL/настроек, а не кода | `COOKIE_SECURE=false` дефолт (включать только для HTTPS). COOKIE_SECURE env + .env.example | — |
| 2026-08-19 | game | GM вызывал `get_rolls` в начале каждого ответа, хотя броски уже в контексте | Промт требовал «FIRST call get_player_sheet and get_rolls» | Промты убрали get_rolls из FIRST-инструкций: только для старых/спорных | G36 |
| 2026-08-19 | game, perf | GM отвечал 59–306 сек (10–19 LLM-вызовов на ответ) | 1) Pass 2 перечитывал данные, уже прочитанные Pass 1 (до 6 дублей на батч). 2) Мозг-индекс 13.2 КБ прелоажен целиком в каждый вызов. 3) read_document без лимита тащил секции 25 КБ. 4) get_chat_summary звался в каждом батче. 5) порог компрессии 89.6K никогда не срабатывал | 1) `buildStudySummary` — результаты Pass 1 в Pass 2. 2) get_chat_summary убран из Pass 1, пустой саммари пишется явно. 3) GM read_document кап 8000. 4) get_chat_summary из Pass 1 + промпты. 5) порог `Math.min(limit×0.7, 24000)`. Промпты ужаты 15.6→14.1 КБ. Итог: 70 сек, 4–6 вызовов на ответ | G40, G41 |
| 2026-08-19 | builder, perf | Билдер думал 353 сек на exec-фазе и читал по 4 раза один документ | Модель запрашивала `read_document(limit: 26000)`; жёсткий кап `Math.min(limit ?? 5000, 8000)` заставил дробить 25 КБ документ на 4 вызова (offset 0/8000/16000/24000) — 4 LLM-вызова на документ вместо 1. Кап был нужен GM, но навредил билдеру | Builder `read_document` — полное чтение при отсутствии offset/limit; GM-тул сохранил кап 8000 | G41 |
| 2026-08-19 | builder, docs | Билдер правил не ту игру: обновил индекс «DnD test 2», а активной была «dnd test 3» | В БД по копии индекса/секций на каждую мастер-игру; инструкции содержали ID из неактивной игры (`a158e9c9`), а активная (`bfd66f20`) осталась со старым индексом и 19 битыми ссылками | Инструкции билдеру — после `getActiveGame()`; активный индекс `22a57c82` обновлён, 19 ссылок `[[b7208b3d...|rules/mechanics]]` перенаправлены на новые секции | G43 |
| 2026-08-19 | builder, docs | Трейс тул-аргументов пустой (`args: {}` у всех 343 вызовов) | AI SDK v7 кладёт аргументы tool call'а в `call.input`, код читал `call.args` | `JSON.stringify(call.input ?? call.args ?? {})` в gm-runner и builder-runner; + реальный elapsedMs через `performance.now()` | G39 |
| 2026-08-19 | builder, docs | Мозг `rules/mechanics` 25 КБ одним файлом; индекс 13.2 КБ со сводками | Мозг создавался без требований к структуре — билдер делал одну большую секцию и индекс-дубль | Гайд билдера (topic `brain`): секции ≤6–7 КБ, индекс 3–5 КБ навигация+политика, одна тема — одно место, без дублей; после разбивки — обновить роутер и wiki-ссылки | G42 |

## Legend
- **Area:** auth, builder, chat, db, ui, i18n, admin, sidebar, game
- **Guard:** что предотвратит повторение (правило, линтер, тест, документация)
